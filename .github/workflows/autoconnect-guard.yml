name: Auto-Connect Guard

# Fails PRs that break the autoconnect / audit-report pipeline.
# Runs on any change to those modules, their tests, or the /auto-connect route.

on:
  push:
    branches: [main]
    paths:
      - "src/lib/autoconnect/**"
      - "src/routes/auto-connect.tsx"
      - ".github/workflows/autoconnect-guard.yml"
  pull_request:
    paths:
      - "src/lib/autoconnect/**"
      - "src/routes/auto-connect.tsx"
      - ".github/workflows/autoconnect-guard.yml"

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install
        run: bun install --frozen-lockfile
      - name: Typecheck (fails on any new TS error project-wide)
        run: bun run typecheck
      - name: Autoconnect e2e (cancel, SSE cancel-sql, audit bundle, checksum mismatch)
        run: bunx vitest run src/lib/autoconnect/e2e-cancel.spec.ts src/lib/autoconnect/sse-cancel-sql.spec.ts src/lib/autoconnect/audit-bundle.spec.ts src/lib/autoconnect/verify-mismatch.spec.ts
      - name: Build
        run: bun run build
